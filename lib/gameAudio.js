var buzz = require('node-buzz');

function GameAudio(config) {
  this.tracks = {};

  var soundsFilePath = config.soundDirectory;
  function addTrack (trackName, filePath) {
    
  });
}

module.exports = GameAudio;
