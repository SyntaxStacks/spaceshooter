var reqs = ['Buzz', 'Config'];

define( reqs, function( Buzz, Config ) {
  function GameAudio() {
    this.tracks = {};

    var soundsFilePath = Config.soundDirectory;
    function addTrack (trackName, filePath) {
      
    });

    

  }

  return( GameAudio );
});
