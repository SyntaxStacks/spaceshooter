define(function() { 
  
  function Config() {
    var config = {
      frameHeight: 300,
      frameWidth: 600
    };

    this.frameHeight = config.frameHeight;
    this.frameWidth = config.frameWidth;
  }

  return Config; 
}); 


